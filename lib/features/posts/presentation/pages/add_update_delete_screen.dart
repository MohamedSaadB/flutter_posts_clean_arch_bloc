import 'package:flutter/material.dart';

class AddUpdateDeleteScreen extends StatelessWidget {
  const AddUpdateDeleteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
